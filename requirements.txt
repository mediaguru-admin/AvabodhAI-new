aiofiles==25.1.0
aiohappyeyeballs==2.6.2
aiohttp==3.13.5
aiosignal==1.4.0
alembic==1.18.4
annotated-doc==0.0.4
annotated-types==0.7.0
antlr4-python3-runtime==4.9.3
anyio==4.13.0
attrs==26.1.0
backoff==2.2.1
beautifulsoup4==4.14.3
blinker==1.9.0
cachetools==7.1.4
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.1
colorama==0.4.6
contourpy==1.3.3
cryptography==48.0.0
cycler==0.12.1
dataclasses-json==0.6.7
distro==1.9.0
emoji==2.15.0
et_xmlfile==2.0.0
fastapi==0.136.3
filelock==3.29.0
filetype==1.2.0
flatbuffers==25.12.19
fonttools==4.63.0
frozenlist==1.8.0
fsspec==2026.4.0
gitdb==4.0.12
GitPython==3.1.50
greenlet==3.5.1
h11==0.16.0
html5lib==1.1
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
httpx-sse==0.4.3
idna==3.16
itsdangerous==2.2.0
Jinja2==3.1.6
jiter==0.15.0
joblib==1.5.3
jsonpatch==1.33
jsonpointer==3.1.1
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
kiwisolver==1.5.0
langchain==1.3.1
langchain-classic==1.0.7
langchain-community==0.4.2
langchain-core==1.4.0
langchain-experimental==0.4.2
langchain-openai==1.2.2
langchain-ollama
langchain-protocol==0.0.15
langchain-text-splitters==1.1.2
langsmith==0.8.5
llvmlite==0.47.0
lxml==6.1.1

# ── HTML table -> Markdown (pipeline/memory.py, POSSIBLY HELPFUL TABLES) ─────
# pandas.read_html() correctly resolves rowspan/colspan into a proper
# MultiIndex column structure — tested against markdownify (already a
# dependency, via pipeline/scraper.py) and a hand-rolled BeautifulSoup
# converter, both of which misalign a table's rows the moment a header
# spans multiple rows (a real pattern in the tables we ingest). tabulate
# is what DataFrame.to_markdown() uses under the hood — not pulled in by
# pandas automatically.
pandas
tabulate
Mako==1.3.12
Markdown==3.10.2
markdown-it-py==4.2.0
MarkupSafe==3.0.3
marshmallow==3.26.2
mdurl==0.1.2
multidict==6.7.1
mypy_extensions==1.1.0
narwhals==2.21.2
numpy==2.4.6
# NOT a transitive dependency, despite being required at runtime: unstructured
# 0.27.1 uses NLTK to tokenise DOCX and some text formats but declares no
# dependency on it (verified against its wheel METADATA - zero Requires-Dist
# lines mention nltk). Both the Dockerfile and doc/SETUP.md run
# `python -m nltk.downloader`, which fails outright without this.
#
# Added 2026-08-23 after a clean-venv rebuild: the previous environment only
# had nltk because an older resolution once pulled it in, so a fresh
# `pip install -r requirements.txt` produced an install that could not parse
# DOCX and could not complete the documented setup steps.
nltk==3.9.4
openai==2.38.0
orjson==3.11.9
ormsgpack==1.12.2
packaging==26.2
# pdfplumber REMOVED 2026-08-23. It was never imported by any source file in
# this repo - PDF parsing goes through unstructured.partition.pdf
# (pipeline/extractor.py), which declares its own pdfminer.six.
#
# Keeping it made requirements.txt uninstallable from a clean environment:
# pdfplumber 0.11.7 pins pdfminer.six==20250506 exactly, while
# unstructured[all-docs] 0.27.1 requires pdfminer-six>=20251230 - no version
# satisfies both, so pip exits with ResolutionImpossible. The existing
# environment only worked because it had been built up incrementally before
# those ranges diverged; a fresh `pip install -r requirements.txt` failed.
#
# pdfminer.six is left unpinned deliberately so pip resolves it from
# unstructured's own declared range.
pillow==12.2.0
propcache==0.5.2
proto-plus==1.28.0
protobuf==6.33.6
psutil==7.2.2
psycopg2-binary==2.9.12
pyarrow==24.0.0
pyasn1==0.6.3
pyasn1_modules==0.4.2
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic_core==2.46.4
Pygments==2.20.0
pypandoc==1.17
pyparsing==3.3.2
pypdf==6.12.1
pypdfium2==5.8.0
python-dateutil==2.9.0.post0
python-docx==1.2.0
python-dotenv==1.2.2
python-magic==0.4.27
python-multipart==0.0.29
PyYAML==6.0.3
referencing==0.37.0
regex==2026.5.9
requests==2.34.2
requests-toolbelt==1.0.0
rich==15.0.0
rpds-py==0.30.0
scipy==1.17.1
setuptools==81.0.0
shellingham==1.5.4
six==1.17.0
smmap==5.0.3
sniffio==1.3.1
soupsieve==2.8.4
SQLAlchemy==2.0.50
starlette==1.1.0
sympy==1.14.0
tenacity==9.1.4
threadpoolctl==3.6.0
tiktoken==0.13.0
toml==0.10.2
tqdm==4.67.3
typer==0.25.1
typing-inspect==0.9.0
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2026.2
# 2026-08-22: upgraded from 0.18.32 -> 0.27.1 (isolate_table/skip_table_chunking
# need >=0.23.0 — see pipeline/chunker.py). Using [all-docs] rather than the
# narrower [pdf,docx,pptx,xlsx,md] extras — matches what was actually
# installed and confirmed working live (real 40-page-PDF ingestion,
# hi_res, 24 tables, 17 images, no crash) after an earlier isolated-script
# test of the same version appeared to hang; the real app under uvicorn did
# not reproduce that, so treating it as environment/invocation-specific,
# not a real 0.27.1 defect. If hi_res hangs return in the live app, revert
# to unstructured[pdf,docx,pptx,xlsx,md]==0.18.32 (drop isolate_table too —
# unavailable there) and pandas==3.0.5.
# Explicit pins because unstructured[all-docs] does NOT provide these on
# Windows. Its own metadata gates them:
#     unstructured-inference ... platform_system != 'Windows'
#     torch ................... platform_system != 'Windows' and extra == 'huggingface'
#     transformers ............ extra == 'huggingface'   (all-docs does not include it)
#
# But this app needs all three regardless of platform: hi_res PDF parsing
# uses unstructured_inference's layout model, and its table-structure model
# is a torch/transformers TableTransformer (see main.py::_warm_table_model).
#
# Found 2026-08-23 by rebuilding .venv from scratch on Windows - the old
# environment only had them because an earlier resolution once pulled them
# in. A clean install produced an app that could not parse PDFs.
#
# torch: install the CPU build to avoid pulling ~2GB of CUDA wheels that
# nothing here uses (no GPU code path exists). On Linux/Windows:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers==4.57.6
unstructured-inference==1.6.12
unstructured[all-docs]==0.27.1
urllib3==2.7.0
uuid_utils==0.16.0
uvicorn==0.48.0
watchdog==6.0.0
watchfiles==1.2.0
webencodings==0.5.1
websockets==16.0
wrapt==2.2.1
xxhash==3.7.0
yarl==1.24.2
zstandard==0.25.0

# ── Web scraping ─────────────────────────────────────────────────────────────
playwright
markdownify

# ── /kb subsystem's PDF loader (langchain_community.PyMuPDFLoader) ───────────
# NOT removed despite the note below about pymupdf — that note was written
# assuming pipeline/loader.py would be deleted entirely; it wasn't. loader.py
# is /kb-only now (see its module docstring) and still uses PyMuPDFLoader for
# .pdf files, which needs this package installed. The MAIN pipeline uses
# unstructured instead (pipeline/extractor.py) and never touches this.
pymupdf

# ── Vector search (Qdrant migration) ─────────────────────────────────────────
qdrant-client
# fastembed: SPLADE (sparse) embeddings AND reranking (TextCrossEncoder) —
# ONNX runtime, no torch. RERANK_MODEL (config/settings.py) must be a model
# TextCrossEncoder.list_supported_models() actually lists — tried BAAI's own
# FlagEmbedding runtime for a model outside that list once (2026-08-21) and
# reverted: too heavy for a resource-constrained dev box (page-file failure,
# a transformers version pin, a confirmed-live 392s first-request stall).
fastembed

# ── /kb subsystem (pipeline/kb_pipeline.py, out of scope for this migration) ──
# Missing from the original UTF-16 requirements.txt entirely, and not
# installed in any venv found on this machine — pinned here to the latest
# PyPI release compatible with this repo's langchain-core==1.4.0 pin
# (langchain-postgres 0.0.17 requires langchain-core>=0.2.13,<2.0, checked
# 2026-08-21). If a different version was actually running in whatever
# environment this was deployed from, confirm and adjust.
langchain-postgres==0.0.17

# ── Testing ───────────────────────────────────────────────────────────────────
pytest

# ── Removed 2026-08-21 (Qdrant migration): pgvector, streamlit ──────────────
# pgvector    -> DocumentChunk/pgvector dropped; chunk storage moves to Qdrant.
#                Do not re-add unless Postgres regains a vector column.
# streamlit   -> app.py (the Streamlit UI) is being deleted; it called
#                pipeline functions directly with no tenant context.
# (pymupdf is NOT removed — see the /kb subsystem note above.)
# NOTE: langchain-experimental / langchain-community / langchain-postgres are
# KEPT despite Vijay's docs recommending their removal — pipeline/kb_pipeline.py
# (the separate, out-of-scope /kb subsystem, see api/routes/kb.py) depends on
# them directly (SemanticChunker, PGVectorStore) and is not part of this
# migration. Re-evaluate only if /kb is ever migrated or removed.
boto3==1.43.78
